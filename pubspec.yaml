name: colormeup
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.1

flutter:
  uses-material-design: true
